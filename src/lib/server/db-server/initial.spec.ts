import { describe, it } from 'vitest';

describe('db initialised', () => {
	it('check', () => {
		console.log('db started');

		// --------------------
		// Construct Tx (with Alice commitment)

		// Bob sign (partial sign + commitment)
		// Alice (use Bob commitment -> sign )
		//----------------------
		// Alice sign (1 input)
		//----------------------
	});
});
